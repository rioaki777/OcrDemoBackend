ktor {
    development = true
    deployment {
        port = 8080
        port = ${?PORT}
        watch = [ classes, resources ]
    }
    application {
        modules = [ com.rioaki.ocrdemo.ApplicationKt.module ]
    }
    uploadDir = "C:/TestFolder" # クライアントからアップされたファイルを配置するフォルダ
    pythonDir = "C:/.../python.exe" # python.exeのパス
}